
export default class Configure{
	constructor(config) {
		/**
		 * config 中保存webpack打包配置中的各种基础配置
		 * @entry string|object
		 * @output object
		 * @loader object
		 * @plugins object[]
		 */
		this.config = config
	}

	configure() {
		//
	}
}